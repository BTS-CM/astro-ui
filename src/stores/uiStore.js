// basketStore.js handles persistent basket data.
// This uiStore is for non-persistent UI states if needed elsewhere.
// For now, it's empty after removing dialog control states.

// Example of other potential UI states (unrelated to dialogs discussed):
// import { atom } from 'nanostores';
// export const $isSidebarOpen = atom(false);
