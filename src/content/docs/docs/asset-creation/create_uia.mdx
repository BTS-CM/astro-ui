---
title: Create UIA
description: Issue a new user-issued asset with custom flags, permissions and fees using asset_create.
sidebar:
  order: 1
---

import { Card, CardGrid, Aside, Steps, Tabs, TabItem } from '@astrojs/starlight/components';

**Create UIA** issues a new **user-issued asset (UIA)** — a fully customizable token whose supply and rules you control as the issuer. You set the symbol, precision, supply caps, market fees, permissions and flags, plus an optional white/blacklist and a core exchange rate.

**Open in the app:** [Create UIA](/create_uia.html).

## What it does

<CardGrid>
  <Card title="Custom token" icon="gem">
    Define symbol, precision and supply of your own asset.
  </Card>
  <Card title="Permissions & flags" icon="lock">
    Control transfer, market, and other on-chain flags.
  </Card>
  <Card title="Market fees" icon="coins">
    Optionally charge a market fee and set a max market fee.
  </Card>
</CardGrid>

## How to use

<Steps>

1. Open **Create UIA** from the homepage under *Asset Creation*.

2. Enter the symbol, precision and description, and supply caps.

3. Optionally enable the **NFT section** to attach NFT metadata (title, artist, narrative, tags, type, attestation, licences, IPFS media) — stored inside the description JSON (see below).

4. Configure market fee percent, max market fee, issuer permissions and flags (transferable, marketable, etc.).

5. Optionally set whitelist/blacklist authorities and markets, plus a core exchange rate.

6. Authorize. The app broadcasts `asset_create`.

</Steps>

<Aside type="note" title="Lifetime member">
  On BitShares, issuing assets generally requires **Lifetime Member (LTM)** status. Upgrade first via [Lifetime Membership](/docs/settings/ltm.html) if you see a permission error.
</Aside>

<Aside type="tip" title="NFT metadata lives in the description">
  With the NFT toggle on, the `description` field carries an `nft_object` alongside the main text — e.g. `{"main":"...","short_name":"...","market":"...","nft_object":{"title":"...","artist":"...","narrative":"...","tags":"...","type":"NFT/ART/VISUAL","attestation":"...","acknowledgements":"...","holder_license":"...","license":"...","media":[...]}}`. Any asset — UIA or SmartCoin — can carry this. See [Assets & Tokens](/docs/learn/assets.html).
</Aside>

## Operations used

Create UIA emits different operations depending on whether you are creating a new asset or editing an existing one.

<Tabs>
  <TabItem label="asset_create">

Creating a new UIA emits `asset_create` (operation `10`):

```json
{
  "issuer": "1.2.x",
  "symbol": "MYUIA",
  "precision": 5,
  "common_options": {
    "description": "{\"main\":\"My user-issued asset\"}",
    "max_supply": 100000000000000,
    "market_fee_percent": 0,
    "max_market_fee": 0,
    "issuer_permissions": 0,
    "flags": 0,
    "core_exchange_rate": {
      "base":  { "amount": 100000, "asset_id": "1.3.0" },
      "quote": { "amount": 100000, "asset_id": "1.3.1" }
    },
    "whitelist_authorities": [],
    "blacklist_authorities": [],
    "whitelist_markets": [],
    "blacklist_markets": [],
    "extensions": {}
  },
  "extensions": {}
}
```

| Field | Meaning |
| --- | --- |
| `issuer` | Your account (`1.2.x`). |
| `symbol` / `precision` | Token ticker and decimal places. |
| `max_supply` / `max_market_fee` | Integer amounts (× precision). |
| `market_fee_percent` | Hundredths of a percent (e.g. `100` = 1%). |
| `issuer_permissions` / `flags` | Bitmask of permission/flag bits. |
| `core_exchange_rate` | Reference price vs BTS (`1.3.0`). |

  </TabItem>
  <TabItem label="asset_update">

Editing an existing UIA emits `asset_update` (operation `11`) with a `new_options` block:

```json
{
  "issuer": "1.2.x",
  "asset_to_update": "1.3.x",
  "new_options": {
    "description": "{\"main\":\"Updated description\"}",
    "max_supply": 100000000000000,
    "market_fee_percent": 100,
    "max_market_fee": 500000,
    "issuer_permissions": 0,
    "flags": 0,
    "core_exchange_rate": {
      "base":  { "amount": 100000, "asset_id": "1.3.0" },
      "quote": { "amount": 100000, "asset_id": "1.3.x" }
    },
    "whitelist_authorities": [],
    "blacklist_authorities": [],
    "whitelist_markets": [],
    "blacklist_markets": [],
    "extensions": {}
  },
  "extensions": {}
}
```

| Field | Meaning |
| --- | --- |
| `asset_to_update` | The `1.3.x` asset object id. |
| `new_options` | Replacement common_options (same shape as create). |

  </TabItem>
</Tabs>

## Related pages

- [Create SmartCoin](/docs/asset-creation/create_smartcoin.html)
- [Issued Assets](/docs/asset-creation/issued_assets.html)
- [Lifetime Membership](/docs/settings/ltm.html)
