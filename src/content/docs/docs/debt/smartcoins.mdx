---
title: SmartCoins
description: Browse market-pegged assets (MPAs) and open their margin-borrow pages. Read-only overview.
sidebar:
  order: 3
---

import { Card, CardGrid, Aside, Steps } from '@astrojs/starlight/components';

**SmartCoins** is a read-only browser of **market-pegged assets (MPAs)** — smartcoins whose value tracks an external reference (e.g. USD, EUR, gold) via collateralized debt positions and price feeds. From here you can inspect each smartcoin's health metrics and jump into its borrow page.

**Open in the app:** [SmartCoins](/smartcoins.html).

## What it does

<CardGrid>
  <Card title="Market overview" icon="chart">
    List all MPAs with status, feed and ratio badges.
  </Card>
  <Card title="Health metrics" icon="shield">
    See MCR, MSSR, ICR and the latest price feed at a glance.
  </Card>
  <Card title="Drill down" icon="arrow-right">
    Open any smartcoin's borrow/manage page (`/smartcoin?id=1.3.x`).
  </Card>
</CardGrid>

## How to use

<Steps>

1. Open **SmartCoins** from the homepage under *Forms of Debt*.

2. Filter or search the list of market-pegged assets.

3. Review each card's MCR / MSSR / ICR / feed badges.

4. Click a smartcoin to open its detail/borrow page, where you can open or adjust a CDP (via `call_order_update` — see [Borrow](/docs/debt/borrow.html)).

</Steps>

<Aside type="note" title="View, don't broadcast">
  This browser emits **no operations**. Creating or updating a smartcoin's feed/parameters happens elsewhere; borrowing against one uses `call_order_update` from the smartcoin detail page.
</Aside>

## Publishing price feeds

SmartCoins track their peg through **price feeds** published by authorized feed producers (typically witnesses). If you are a feed producer for an asset, the **Publish Feed** screen (`/publish_feed.html?asset=1.3.x`) pre-fills the current/median feed and lets you submit an update: settlement price, maintenance collateral ratio (MCR), short-squeeze ratio (MSSR), and core exchange rate.

Publishing broadcasts `asset_publish_feed`:

```json
{
  "publisher": "1.2.x",
  "asset_id": "1.3.x",
  "feed": {
    "settlement_price": {
      "base": { "amount": 100000, "asset_id": "1.3.x" },
      "quote": { "amount": 30000000, "asset_id": "1.3.0" }
    },
    "maintenance_collateral_ratio": 1750,
    "maximum_short_squeeze_ratio": 1100,
    "core_exchange_rate": {
      "base": { "amount": 100000, "asset_id": "1.3.x" },
      "quote": { "amount": 30000000, "asset_id": "1.3.0" }
    }
  },
  "extensions": {}
}
```

| Field | Meaning |
| --- | --- |
| `publisher` | Your account — must be an authorized feed producer for the asset. |
| `asset_id` | The smartcoin being fed (`1.3.x`). |
| `feed.settlement_price` | Fair price (base = asset, quote = collateral). |
| `maintenance_collateral_ratio` | MCR × 10 (e.g. `1750` = 175%). |
| `maximum_short_squeeze_ratio` | MSSR × 10 (e.g. `1100` = 110%). |

## Related pages

- [Borrow](/docs/debt/borrow.html)
- [Bid on settlement](/docs/debt/settlement_bids.html)
- [Lend](/docs/debt/lend.html)
- [Assets & Tokens](/docs/learn/assets.html)
- [Borrowing, Shorting & Margin Calls](/docs/learn/margin.html)
